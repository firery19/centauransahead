AQU_ring_of_fire = {
	aquila_southern_forts = {
		icon = GFX_decision_revolt
		visible = {
			tag = AQU
			NOT = { has_country_flag = AQU_dismantled_ring_of_fire }
		}
		available = {
			NOT = { has_country_flag = AQU_dismantled_ring_of_fire }
		}
		selectable_mission = no
		is_good = no
		days_mission_timeout = 180
		fire_only_once = no
		ai_will_do = { factor = 100 }

		timeout_effect = {
        declare_war_on = {
            target = FRA
            type = annex_everything
			}
		}

		complete_effect = {
			 
		}

		remove_effect = {
			
		}
	}
	
	dismantle_e_rof = {

		icon = generic_construction

		allowed = {
			always = yes
		}

		cost = 50
		fire_only_once = yes

		days_remove = 200

		available = {
			controls_state = 37
		}

		visible = {
			num_of_factories < 60
		}

		modifier = {
			civilian_factory_use = 5
		}

		complete_effect = {
			37 = {
				set_building_level = {
					type = bunker
					level = 0
					province = {
						all_provinces = yes
						level > 0
					}
				}
			}
		}
	}
	
	dismantle_s_rof = {

		icon = generic_construction

		allowed = {
			always = yes
		}

		cost = 50
		fire_only_once = yes

		days_remove = 200

		available = {
			controls_state = 31
			controls_state = 35
		}

		visible = {
		always = yes
		}

		modifier = {
			civilian_factory_use = 5
		}

		complete_effect = {
			31 = {
				set_building_level = {
					type = bunker
					level = 0
					province = {
						all_provinces = yes
						level > 0
					}
				}
			}
			35 = {
				set_building_level = {
					type = bunker
					level = 0
					province = {
						all_provinces = yes
						level > 0
					}
				}
			}
		}
	}
}