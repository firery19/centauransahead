# Allows you to create your own dynamic keys
# to be called in localization.
# defined_text -> this is it, we're defining the text
# text -> a discrete entry that can be picked to display in loc.
# trigger -> determines if a text entry will be picked or not.
# (The triggers need to be valid for the scope the key is called in
# (eg Root or From.From).)
# localization_key -> points to the localization key
# that'll be used if trigger passes
# random_list can take a scoped variable as a seed if you don't want a predictable result

defined_text = { # Country
	name = GetImportOrDomesticFilmIndustry
	text = {
		trigger = {
			is_major = yes
		}
		localization_key = DOMESTIC_FILM_INDUSTRY
	}
	text = {
		localization_key = IMPORTED_FILM_INDUSTRY
	}
}

defined_text = { # Country
	name = GetImportOrDomesticFilmIndustryDesc
	text = {
		trigger = {
			is_major = yes
		}
		localization_key = DOMESTIC_FILM_INDUSTRY_DESC
	}
	text = {
		localization_key = IMPORTED_FILM_INDUSTRY_DESC
	}
}

defined_text = {
	name = GetCountryAppropriateFilmProduction
	
	###Hydra films
	text = {
		trigger = {
			tag = HYS
		}
		random_list = {
			seed = num_of_controlled_factories #Because otherwise seed is drawn directly from the country - it'll be the same each time.
			10 = { localization_key = hydrus_film_1 }
			10 = { localization_key = hydrus_film_2 }
			10 = { localization_key = hydrus_film_3 }
		}
	}
	text = {
		trigger = {
			if = {
				limit = { is_major = yes }
				tag = ORI
			}
			else = {
				is_in_faction_with = ORI
			}
		}
		random_list = {
			seed = num_of_controlled_factories #Because otherwise seed is drawn directly from the country - it'll be the same each time.
			10 = { localization_key = orion_film_1 }
			10 = { localization_key = orion_film_2 }
			10 = { localization_key = orion_film_3 }
		}
	}
	text = {
		trigger = {
			if = {
				limit = {
					is_major = yes
				}
				tag = RIA
			}
			else = {
				is_in_faction_with = RIA
			}
		}
		random_list = {
			seed = num_of_controlled_factories #Because otherwise seed is drawn directly from the country - it'll be the same each time.
			10 = { localization_key = riaa_film_1 }
			10 = { localization_key = riaa_film_2 }
			10 = { localization_key = riaa_film_3 }
		}
	}
	### Ursa
	text = {
		trigger = {
			if = {
				limit = {
					is_major = yes
				}
				tag = URS
			}
			else = {
				is_in_faction_with = URS
			}
		}
		random_list = {
			seed = num_of_controlled_factories #Because otherwise seed is drawn directly from the country - it'll be the same each time.
			10 = { localization_key = ursa_film_1 }
			10 = { localization_key = ursa_film_2 }
			10 = { localization_key = ursa_film_3 }
		}
	}
	
	### Axis films
	
	text = {
		trigger = {
			if = {
				limit = {
					is_major = yes
				}
				tag = ANT
			}
			else = {
				is_in_faction_with = ANT
			}
		}
		random_list = {
			seed = num_of_controlled_factories #Because otherwise seed is drawn directly from the country - it'll be the same each time.
			10 = { localization_key = antares_film_1 }
			10 = { localization_key = antares_film_2 }
			10 = { localization_key = antares_film_3 }
		}
	}
	
	text = {
		trigger = {
			if = {
				limit = {
					is_major = yes
				}
				tag = COV
			}
			else = {
				is_in_faction_with = COV
			}
		}
		random_list = {
			seed = num_of_controlled_factories #Because otherwise seed is drawn directly from the country - it'll be the same each time.
			10 = { localization_key = corvus_film_1 }
			10 = { localization_key = corvus_film_2 }
			10 = { localization_key = corvus_film_3 }
		}
	}
	
}
